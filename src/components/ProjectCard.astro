---
import type { Project } from "@/data/projects";

type Props = Project;

const { title, description, link, isComingSoon } = Astro.props;

---

<>
  {
    isComingSoon && (
      <div class="t group flex w-full flex-col justify-between gap-2 md:flex-row md:items-center">
        <div class="flex flex-col gap-2 md:flex-row md:items-center md:gap-4">
          <p class="underline decoration-1 duration-300 hover:decoration-green-400">{title}</p>
          <p class="text-gray-600">{description}</p>
        </div>

        <p class="w-fit rounded-md bg-neutral-950 text-white px-4 py-1">🌱 Soon</p>
      </div>
    )
  }

  {
    !isComingSoon && (
      <a
        class="group flex w-full cursor-pointer flex-col justify-between gap-2  md:flex-row md:items-center"
        href={link}
        target="_blank"
        >
        <div class="flex flex-col gap-2 md:flex-row md:items-center">
          <p class="underline decoration-1 duration-300 hover:decoration-green-400">{title}</p>
          <p class="text-gray-600">{description}</p>
        </div>
      </a>
    )
  }
</>
